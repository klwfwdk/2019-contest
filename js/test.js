//程序文件Box.java //本程序在NetBeansIDE7.4及java1.7环境编写 并运行通过 importjava.util.*; 
async function curState(curmap){
    const w=16,h=16;
    let count3=0;
    for (let i = 0; i < curmap.length; i++) {
        const line = curmap[i];
        for (let j = 0; j < element.length; j++) {
            const point = element[j];
            if (point===3) {
                count3++;
                break;
            }
        }
    }
    
}